---
name: master-control
description: A visual GUI dashboard for managing local OpenClaw instances. Monitor config health, run diagnostics, and manage system settings via a web interface.
author: anervalens-netizen
tags: [gui, dashboard, management, tool, system, react]
---

# OpenClaw Master Control

The missing GUI for your local AI agent. No more JSON editing or blind CLI commands.

## ✨ Features
- **Config Health Check**: Real-time validation of `openclaw.json`.
- **Visual Management**: Edit providers and settings visually.
- **System Maintenance**: Run `doctor` and `update` with one click.
- **Secure Local Bridge**: Runs entirely on your machine via Node.js.

## 🚀 Usage

### Install via Agent
"Install the 'Master Control' GUI to manage my system."

### Manual Install
```bash
git clone https://github.com/anervalens-netizen/openclaw-master-control.git
cd openclaw-master-control
npm install
npm run start:all
```

Then open `http://localhost:5173`.
